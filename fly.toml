# fly.toml app configuration for Telegram bot (polling)
# No HTTP, no ports, no autosuspensión

app = "repo-bot"
primary_region = "gru"

[build]
  # El Dockerfile será detectado automáticamente

# Ejecutar el bot como proceso principal (sin servidor HTTP)
[processes]
  bot = "python bot_gastos.py"

# Configuración de la VM
[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1
